def credential(blockchain, address, key, user='', password=''):
    return {
        'blockchain': blockchain,
        'address': address,
        'key': key,
        'user': user,
        'password': password
    }
