from enum import Enum, auto

class Blockchain(Enum):
    ETHEREUM = auto()
    MULTICHAIN = auto()
    BITCOIN = auto()
    POSTGRES = auto()
    STELLAR = auto()
    HYPERLEDGER = auto()
    EOS = auto()
    IOTA = auto()
    MONERO = auto()
    LITECOIN = auto()
    RIPPLE = auto()
